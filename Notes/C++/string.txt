pos~~idx, iter

string a;
a.insert(pos, str);
a.insert(pos, str, subpos, cnt);

a.replace(pos, str);
a.erase(pos_1, pos_2);
a.clear();

string a, b;
b.swap(a);

string a, b;
a.compare(b)
b.compare(a)
b.compare(pos_b, cnt_b, a)
b.compare(pos_b, cnt_b, a, pos_a, cnt_a)

string a;
a.substr(pos, len)

string a, b;
b.find(a)
b.find(a, pos) // pos부터 검색
b.find(a, pos, num)
b.find(a)!=string::npos

string::npos // no position

cin.ignore();
getline(cin, s);

//
char c;
if(isdigit(c)){
    ;
}

// delimiter, whitespace (delimiter and whitespace are not exactly the same, but whitespace is often used as a delimiter)
space: ' '
tab: '\t'
newline: '\n'

//
string s;
cin>>s; => leaves the delimiter(whitespace) character in the input buffer / automatically skips leading delimiter(whitespace) but leaves trailing delimiter(whitespace) in the input buffer
getline(cin, s); => reads and consumes the newline character, but does not store it in the resulting string

//
'' ~~ char
"" ~~ char[] ~~ char*
   ~~ string